import { Button, Input } from "@whole-ui/components";

import createReactWrapper from "./create-react-wrapper";

export const Components = {
  Button: createReactWrapper(Button),
  Input: createReactWrapper(Input),
};
